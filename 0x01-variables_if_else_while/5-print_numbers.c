#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - Entry point
*
* description is  prints all single digit numbers
*
* Return: (0); (Success)
*/
int main(void)
{
int dio = 0;
while (dio <= 9)
{
printf("%d", dio);
dio++;
}
printf("\n");
return (0);
}
