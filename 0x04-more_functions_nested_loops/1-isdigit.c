#include "main.h"
#include <stdio.h>

/**
 * _isdigit - functn check digits
 * @c: input integer
 *
 * Description: check for digits
 * Return: 0, 1 (success)
 */

int _isdigit(int c)
{
{
if (c >= 48 && c <= 57)
}
return (1);
}
{
else
}
{
return (0);
}
